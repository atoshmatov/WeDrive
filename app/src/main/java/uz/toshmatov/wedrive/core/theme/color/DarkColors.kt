package uz.toshmatov.wedrive.core.theme.color

fun darkColors(): WeDriveColors = WeDriveColors(
    background = WHITE,
    itemBackground = Gray20,
    text = WHITE,
    textPrimary = WHITE,
    textGray = GRAY,
    icon = WHITE,
    button = Black30,
    buttonEnable = Gray30,
    buttonContent = Gray40,
    error = Error,
    success = Success,
    textSecondary = BLACK,
    iconChecked = Blue,
    switch = Black20,
    divider = DarkBorder,
    bottomBar = DarkBorder,
    bottomBarIconUnSelected = GRAY,
    bottomBarIconSelected = WHITE,
    placeHolder = GRAY,
)